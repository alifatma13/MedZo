// sanity.config.ts — retained for reference only.
// The Studio is hosted externally at medzo.sanity.studio and is NOT embedded in the Astro site.
// This file is not loaded during astro build.
import { defineConfig } from 'sanity'

export default defineConfig({
  name: 'medzo-studio',
  title: 'MedZo',
  projectId: 'j8vrs6gs',
  dataset: 'production',
})
