---
import { getImage } from "astro:assets";
import heroImageSrc from "../res/hero_image.png";
import Layout from "../layouts/Layout.astro";
import HeroSection from "../components/sections/HeroSection.astro";
import SpecialtyTicker from "../components/sections/SpecialtyTicker.astro";
import ServicesSection from "../components/sections/ServicesSection.astro";
import WhyChooseUs from "../components/sections/WhyChooseUs.astro";
import TestimonialsSection from "../components/sections/TestimonialsSection.astro";
import FAQSection from "../components/sections/FAQSection.astro";
import FinalCTA from "../components/sections/FinalCTA.astro";
import MetricsSection from "../components/sections/MetricsSection.astro";

const heroImageOptimized = await getImage({
	src: heroImageSrc,
	width: 960,
	format: "webp",
	quality: 82,
});

import {
	hero,
	servicesHeading,
	services,
	whyHeading,
	whyItems,
	testimonialsHeading,
	testimonials,
	testimonialsViewMore,
	faqHeading,
	faqs,
	metrics,
	finalCta,
	ticker,
	whyImageAlt,
} from "../lib/content/index";
---

<Layout>
	<link
		slot="head"
		rel="preload"
		as="image"
		href={heroImageOptimized.src}
		type="image/webp"
	/>
	<HeroSection {...hero} />
	<SpecialtyTicker {...ticker} />
	<MetricsSection metrics={metrics} />
	<ServicesSection heading={servicesHeading} services={services} />
	<WhyChooseUs heading={whyHeading} items={whyItems} imageAlt={whyImageAlt} />
	<TestimonialsSection
		heading={testimonialsHeading}
		testimonials={testimonials}
		viewMore={testimonialsViewMore}
	/>
	<FAQSection heading={faqHeading} faqs={faqs} />
	<FinalCTA {...finalCta} />
</Layout>
