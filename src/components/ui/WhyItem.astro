---
// WhyItem.astro — icon + title + description row used in the WhyChooseUs feature list.
import { icons } from "../../lib/utils/icons";
import type { WhyItem } from "../../types/WhyItem";
interface Props extends WhyItem {}
const { iconName, title, description } = Astro.props;
---

<div class="why-item">
  <div class="icon-circle" aria-hidden="true">
    <Fragment set:html={icons[iconName] ?? ""} />
  </div>
  <div class="content">
    <h3 class="item-title">{title}</h3>
    <p class="item-desc">{description}</p>
  </div>
</div>

<style>
  .why-item {
    display: flex;
    gap: 0.875rem;
    align-items: flex-start;
  }
  .icon-circle {
    width: 24px;
    height: 24px;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
    color: var(--color-primary);
    margin-top: 2px;
  }
  .icon-circle svg {
    width: 100%;
    height: 100%;
    display: block;
  }
  .content {
    display: flex;
    flex-direction: column;
    gap: 0.25rem;
  }
  .item-title {
    font-size: var(--text-lg);
    font-weight: var(--weight-semibold);
    color: var(--color-primary);
    margin: 0;
    letter-spacing: var(--tracking-tight);
    line-height: var(--leading-snug);
  }
  .item-desc {
    font-size: var(--text-base);
    color: var(--color-text-body);
    margin: 0;
    line-height: var(--leading-normal);
  }
</style>
